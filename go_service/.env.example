# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=seta_training

# Server Configuration
PORT=8080

# Kafka Configuration (optional - if not set, Kafka events will be disabled)
KAFKA_BROKERS=localhost:9092

# Redis Configuration (optional - if not set, Redis will use defaults)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# JWT Secret for user service communication
ACCESS_TOKEN_SECRET=your_jwt_secret_here

# Node.js User Service URL
USER_SERVICE_URL=http://localhost:4000